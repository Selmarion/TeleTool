# 🛠 Telegram Tools (Telethon)

Набор утилит для автоматизации действий в Telegram с использованием библиотеки Telethon.

## 📋 Содержание
1. [Получение API ключей](#1-получение-api-ключей)
2. [Установка и Настройка](#2-установка-и-настройка)
3. [Описание инструментов](#3-описание-инструментов)
    - [Smart Cleaner (Удаление сообщений)](#smart-cleaner-py)
    - [Media Downloader (Скачивание медиа)](#media-downloader-py)

---

## 1. Получение API ключей

Для работы скриптов необходимо зарегистрировать "приложение" в Telegram. Это бесплатно и делается один раз.

1.  Перейдите на официальный сайт: **[https://my.telegram.org/apps](https://my.telegram.org/apps)**
2.  Введите свой номер телефона и нажмите **Next**.
3.  В Telegram придет служебное сообщение с кодом подтверждения. Введите его на сайте.
4.  Нажмите на ссылку **API development tools**.
5.  Заполните форму **Create new application**:
    -   **App title**: Любое название (например, `MyCleaner`).
    -   **Short name**: Любое короткое имя (например, `mycleaner`).
    -   **URL**: Можно оставить пустым или ввести `http://localhost`.
    -   **Platform**: Выберите `Desktop`.
6.  Нажмите **Create application**.
7.  Скопируйте два значения:
    -   **App api_id** (число)
    -   **App api_hash** (длинная строка)

> **Важно:** Эти данные являются секретными (как пароль). Не передавайте их посторонним.

---

## 2. Установка и Настройка

### Шаг 1: Подготовка окружения
Убедитесь, что у вас установлен Python. Затем установите необходимые библиотеки:

```bash
pip install -r requirements.txt
```

### Шаг 2: Настройка конфигурации (.env)
Создайте файл `.env` (если его нет) в папке проекта и откройте его любым текстовым редактором. Запишите туда полученные ключи:

```ini
# .env файл

# Вставьте сюда данные с my.telegram.org
TG_API_ID=12345678
TG_API_HASH=ваша_длинная_строка_hash

# Имя файла сессии (создается автоматически)
SESSION_NAME=my_session

# === Настройки для Smart Cleaner ===
# Период повторного запуска в секундах (86400 = 24 часа)
SLEEP_PERIOD=86400

# Режим "Сухой прогон" (True - только имитация удаления, False - реальное удаление)
DRY_RUN=False

# Уровень логирования (INFO или DEBUG)
LOG_LEVEL=INFO
```

---

## 3. Описание инструментов

### 🧹 `smart_cleaner.py`
Скрипт для автоматической очистки **ваших** сообщений во всех чатах.

**Возможности:**
*   Сканирует все диалоги.
*   Находит сообщения, отправленные вами.
*   Удаляет их пачками (чтобы избежать бана).
*   **Безопасный режим**: Если в `.env` стоит `DRY_RUN=True`, скрипт просто покажет, сколько сообщений он *мог бы* удалить, но не тронет их.
*   Логирует действия в файл `cleaner.log`.

**Запуск:**
```bash
python smart_cleaner.py
```

---

### 📥 `media_downloader.py`
Скрипт для скачивания фото, видео и документов по ссылке.

**Возможности:**
*   **Обход защиты**: Скачивает медиа, даже если в канале стоит запрет на сохранение (Restricted Content).
*   Поддерживает ссылки на публичные посты (`t.me/channel/123`).
*   Поддерживает ссылки на приватные посты (`t.me/c/123456/123`), если вы состоите в этом чате.
*   Показывает прогресс скачивания.

**Запуск:**
```bash
python media_downloader.py
```
После запуска скрипт попросит ввести ссылку. Вставьте ссылку и нажмите Enter. Файлы сохраняются в папку `downloads/`.
