Скачивайте TeleTool.exe исполнительный файл программы
Работаете от имени администратора.

TeleTool.exe

# 🛠 Telegram Tools (Telethon)

## Описание инструментов

### 🧹 `smart_cleaner.py`
Скрипт для автоматической очистки **ваших** сообщений во всех чатах.

**Возможности:**
*   Сканирует все диалоги.
*   Находит сообщения, отправленные вами.
*   Удаляет их пачками (чтобы избежать бана).
*   **Безопасный режим**: Если в `.env` стоит `DRY_RUN=True`, скрипт просто покажет, сколько сообщений он *мог бы* удалить, но не тронет их.
*   Логирует действия в файл `cleaner.log`.

**Запуск:**
```bash
python smart_cleaner.py
```

---

### 📥 `media_downloader.py`
Скрипт для скачивания фото, видео и документов по ссылке.

**Возможности:**
*   **Обход защиты**: Скачивает медиа, даже если в канале стоит запрет на сохранение (Restricted Content).
*   Поддерживает ссылки на публичные посты (`t.me/channel/123`).
*   Поддерживает ссылки на приватные посты (`t.me/c/123456/123`), если вы состоите в этом чате.
*   Показывает прогресс скачивания.

**Запуск:**
```bash
python media_downloader.py
```
После запуска скрипт попросит ввести ссылку. Вставьте ссылку и нажмите Enter. Файлы сохраняются в папку `downloads/`.
